import numpy as np


def poblacion_inicial(tamaño_poblacion,numero_objetos,peso_maximo,peso_objetos):

    # inicializar matriz de población como un array de numpy
    #poblacion_inicio = 
    i = 0
    #while i<tamaño_poblacion: #generación de individuos aleatorios: vector binario
        #individuo = 
        # obtener el peso del individuo
        #peso_individuo =

        #if peso_individuo < peso_maximo: #verificación si el individuo es factible para ingresar a la población.
            #poblacion_inicio[i] = 
            #i+=1

    return #poblacion_inicio
